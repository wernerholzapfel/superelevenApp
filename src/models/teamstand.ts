
export interface Teamstand {
  "Participant": participant,
  "TotalTeamScore" : string,
  "positie": number
}

export interface participant {
  "Gender": string,
  "Location": string,
  "Name": string,
}
